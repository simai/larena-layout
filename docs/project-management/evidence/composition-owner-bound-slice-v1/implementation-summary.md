Typed owner inputs are projected in memory into existing Framework Recipe inputs; shared artifacts and immutable snapshots remain the existing Layout mechanism.

No Framework grammar or generated source edits.
