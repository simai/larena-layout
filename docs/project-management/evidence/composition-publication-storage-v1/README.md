# Composition publication storage candidate

This is an isolated Layout candidate. It preserves the existing compiled snapshot contract while adding PDO-backed snapshots and transaction-joining artifact publication. A product can update the published artifact pointer and activate its compiled snapshot on one shared database transaction. It is not published, connected to a live route or accepted as product readiness.
