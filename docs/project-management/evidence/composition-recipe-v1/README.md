# Composition Recipe v1

Локальный кандидат проверяет одну существующую статью Larena через точную ревизию
сборщика Simai Framework. Готовый результат сохраняется целым файловым снимком,
переключается по ожидаемой ревизии и откатывается. База данных не меняется.
