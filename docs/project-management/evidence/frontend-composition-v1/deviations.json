{
  "schema": "larena.evidence_deviations.v1",
  "deviations": [
    "The package stage keeps typed content and request-time Auth values as references; owner resolution and login presentation are verified in the Auth application-adoption stage.",
    "The package browser smoke proves server-rendered recursive output at the default Chrome viewport; the full responsive and accessibility matrix is deferred to the real login page.",
    "The repository-wide Specs validator remains blocked by its pre-existing locked Mirai Graph toolchain mismatch; the new standard, graph structure, adapter and Standard Contract checks pass independently."
  ]
}
