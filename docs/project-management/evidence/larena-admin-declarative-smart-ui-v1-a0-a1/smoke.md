# Smoke

The canonical example descriptor loads, normalizes deterministically and keeps
stable component, view, preset, modifier and binding references. Browser smoke
belongs to the integrated Admin checkpoint.
