# Tests

- `composer run quality:gate`
- `composer run test:minimal-cms-layout`
- `composer validate --strict --no-check-publish`

The focused test proves the full hierarchy, deterministic digest and fail-closed unknown region, component and source behavior.
