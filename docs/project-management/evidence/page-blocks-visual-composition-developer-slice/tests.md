# Tests

`PageCompositionRuntimeTest` covers the five catalog entries, order, Smart-view
mapping, unknown type, required settings, unsafe URL and duplicate id rejection.

Full package `composer quality:gate`: PASS on PHP 8.4; PHPStan has no errors.
