# Region inheritance runtime candidate

Local isolated Layout candidate. It now also owns the validated per-region
intent projection used by products to select inherit, replace or explicit empty
and to reset a local choice back to the server registration. It is not
published or accepted as product readiness.
