# Tests

PHP 8.4: complete package test runner passed with exact immutable Framework pair 8c22fe2b80bb / 400d80e501ca configured; no Framework test skipped in final run. New tests cover inheritance, explicit empty, shared block, placement order, provenance, immutable input and nine refusals. Independent PHPStan 2.2.1 level 6 analysis of new runtime: zero errors. Shared ignored vendor links are used; this is not a clean installation proof.

Existing artifact resolver now projects inherited region placements through its same recursive entry implementation. CLI test compiles page → section → externally bound Unicode paragraph to meaningful HTML on the exact old pair. Its lock is 1.0.0 / 894de36…, differing from accepted Specs 1.0.1 / 63daf55…; release conformance remains pending, see framework-contract-delivery-gap.json.

The package runner also covers registered defaults, all three effective modes,
reset, stable ordering, source immutability, unknown-region refusal, invalid-mode
refusal and incomplete-registration refusal.
